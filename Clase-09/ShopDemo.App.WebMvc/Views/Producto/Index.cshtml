@model List<ShopDemo.Core.Entidades.Producto>
@{
    ViewData["Title"] = "Productos";
}

<h1 class="">Productos</h1>

Mensajes:
<span>@ViewData["Mensaje"]</span>
<span>@ViewData["Mensaje2"]</span>
<span>@ViewBag.Mensaje2</span>
<span>@ViewBag.Mensajeee2</span>

<ul>
    @foreach (var producto in Model)
    {
        <li>
                @producto.Nombre | $@producto.Precio
@* 
            <a class="btn btn-primary"
                    asp-controller="Carrito" 
                   asp-action="Agregar" 
                   asp-route-productoId="@producto.ProductoId">+</a> *@

                <form asp-controller="Carrito" 
                      asp-action="Agregar" 
                      method="post">

                    <input type="hidden" 
                           name="productoId" 
                           value="@producto.ProductoId" />

                    <input type="submit" class="btn btn-primary" value="+" />
                 
                </form>
        
        </li>
    }
</ul>